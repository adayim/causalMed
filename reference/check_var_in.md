# Check variables in data

Check if the variables in the data, throw an error with variable names
if not.

## Usage

``` r
check_var_in(vars, data)
```

## Arguments

- vars:

  Variables to check

- data:

  Data set to be checked
